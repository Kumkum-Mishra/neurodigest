# Vercel-optimized requirements (excludes large ML dependencies)
# For local development with full features, use: pip install -r requirements-full.txt
fastapi
uvicorn
mangum
python-multipart
requests
newspaper3k
feedparser
python-dotenv

# Database & Auth
sqlalchemy
sqlmodel
psycopg2-binary
passlib[bcrypt]
python-jose[cryptography]

# Async & APIs
aiohttp

# Utilities
beautifulsoup4
lxml[html_clean]
pydantic[email]
email-validator

# Note: torch, transformers, sentence-transformers, streamlit excluded for Vercel
# These are too large for serverless functions (torch alone is 2GB+)
# Embeddings functionality will use fallback keyword matching on Vercel
# For local dev with full features: pip install -r requirements-full.txt